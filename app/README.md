# Application Folder

This folder is included for the internship repository structure.

Main runnable application files are currently stored in the repository root:

- `app.py` - Flask application
- `streamlit_app.py` - Streamlit application

Run Flask:

```bash
python app.py
```

Run Streamlit:

```bash
python -m streamlit run streamlit_app.py
```
